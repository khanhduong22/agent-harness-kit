{{CORE_RULES}}

## Codex compatibility

- Treat `/opsx`, `/plan`, `/build`, `/test`, `/review`, and `/ship` as intent labels unless an installed skill exposes the same command.
- Repository `AGENTS.md` files add project-specific guidance and override conflicting global defaults when they are closer to the working directory.
